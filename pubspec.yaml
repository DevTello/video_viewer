name: video_viewer
description: Multiplatform minimalist video viewer with spectacular user experience.
homepage: https://github.com/seel-channel/video_viewer
version: 1.2.7
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '3.22.0'

dependencies:
  flutter:
    sdk: flutter
  gesture_x_detector: ^1.1.1
  helpers:
    git:
      url: https://github.com/AMBE1203/helpers
      ref: 91e82e887a668110c528ec85e035a2cfe068f4cb
  http: ^1.3.0
  path_provider: ^2.1.5
  provider: ^6.1.4
  video_player: ^2.9.5
  volume_watcher_plus: ^2.1.3
  video_player_web_hls: ^1.3.0

dependency_overrides:
  video_player_android: 2.7.1

dev_dependencies:
  flutter_test:
    sdk: flutter
